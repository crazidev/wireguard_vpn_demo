name: wireguard_vpn_demo
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.1

dependencies:
  flutter:
    sdk: flutter
  device_preview_plus: ^2.2.6
  flutter_riverpod: ^2.6.1
  flutter_vector_icons: ^2.0.0
  avatar_glow: ^3.0.1
  google_fonts: ^6.2.1
  wireguard_flutter:
    path: ./wireguard_flutter
  animate_do: ^4.2.0
  flutter_svg: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
